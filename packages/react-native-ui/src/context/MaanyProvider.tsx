import type { ReactNode } from 'react';

/**
 * Placeholder provider until the coordinator implementation lands.
 */
export const MaanyProvider = ({ children }: { children: ReactNode }) => {
  throw new Error('MaanyProvider is not implemented yet.');
};
