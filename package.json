{
  "name": "maany-sdk",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "release": "changeset version && pnpm i -w && turbo build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "cosmjs-types": "^0.9.0"
  }
}
